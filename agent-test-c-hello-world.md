---
title: "五个 Agent 的 Hello World 测试"
date: 2026-06-09T21:47:00+08:00
draft: false
---

前几天把主流的几个 agent 工具拉出来测了一圈。起因是我好奇，这些号称"帮你干活"的工具，实际动手能力到底差多少，光看宣传谁都不服谁，不如真刀真枪比一次。

测试方法很简单：用 C 写一个 hello world，编译成可执行文件，再把二进制转成 base64，把这段 base64 丢给每个 agent 去分析，看谁能把程序还原出来。为什么要转 base64？因为对话客户端没法直接传二进制文件，编码成字符串就都能喂了，也算贴近实际用法。

测试对象一共五个：opencode、workbuddy、codex、qoder，还有一个 deepseek 官方网页。前四个是 agent 工具，第五个算对照组，看看不靠工具、纯对话能不能行。

结果有点意思。前面三个 agent 全部卡住了，opencode、workbuddy、codex，不是绕圈子就是凭空给答案，没有一个能正确完成任务。有的假装分析，说了一堆看似专业的话，其实根本没动手；有的干脆说这个不行那个不行，绕来绕去就是不干活。

正确处理的是后面两个：qoder 没有空想，老老实实调用了工具，对 base64 先做解码，再对二进制反编译，最后给出了正确的结果，一步是一步，全对上了。deepseek 官方网页虽然没走 agent 的流程，靠模型自己硬解，也把结果解对了，说明模型本身的底子确实够硬。

为什么差别这么大。我琢磨了一下，问题多半不在模型的智商上，而在 agent 会不会"用工具"。hello world 的反编译这种活，模型记忆里不可能有现成答案，只能靠实际去解码、去读二进制。会调工具的 agent，每一步都做得实；不会调工具的，就只能靠猜，猜得再像，也是编的。

这也正好验证了我上篇博客的想法：弱模型配强 agent，能顶得上强模型。反过来也一样，如果 agent 框架本身不干活，光靠模型强，一样会翻车。工具链和模型，得两边都支棱起来才行。
