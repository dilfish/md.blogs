---
title: "弱模型配强 Agent，也能顶得上强模型"
date: 2026-05-16T20:30:00+08:00
draft: false
---

之前我一直觉得，模型越强越好，强了就是能顶一切。这种印象大概是各家宣传洗出来的，跑分一个比一个高，好像弱一点的模型就没用武之地了。最近换了个思路，把不强的模型配上 workbuddy 这类 agent 工具用了一阵子，发现日常的活，这个组合已经顶得上强模型了。

区别在哪呢。强模型是把能力内化在脑子里，弱模型加 agent，是把能力外化到流程里。一个靠自己，一个靠分工。

弱模型真正弱的地方，是推理链不够长。一个任务拆成十步，每一步它都能做，但要它一口气把十步想清楚，就容易断，中间会忘，会绕，会编。这有点像让一个学生连续做十道题，前几道还行，越往后越糊涂，最后开始瞎写。强模型能干成这件事，是因为它能一次性把这个链条走完，脑子不虚。

agent 干的就是把这个链条接住：拆解任务，调用工具，维护上下文，干完一步还知道回来验证。模型每次只需要想一步，剩下的都是框架在兜底。相当于把一次大模型的长链推理，拆成了若干次短链推理加一个可靠的执行环境。每一步都短，短就不容易出错。

用大白话说，弱模型加 agent，像一个普通程序员配了成熟的工程团队，测试、code review、版本管理都有人做，他只需要把每一段写对。强模型单打独斗，像天才程序员，一个人扛整个项目。大部分时候，前者的交付反而更稳。团队里的螺丝钉水平一般，但流程兜着，出不了大乱子。

当然不是没有代价。agent 的框架是死的，一旦碰到需要模型真正出点新东西的地方，比如想一个没人想到过的解法，agent 帮不上忙，还是得靠模型本身。所以强模型没有被替代，只是被放到了更该用它的地方。日常跑量用弱模型加 agent，真正啃硬骨头的时候再上强模型，两者搭配着用，反而是性价比最高的玩法。

还有个感受是，这个组合把使用门槛也降低了。以前用强模型，你得自己会拆任务，会写提示词，怎么问都是门技术活。现在把活丢给 agent，它自己会拆，你只需要描述清楚想要什么。等于"怎么用好模型"这件事，本身也被外包了。

不过 agent 也只能让执行变得可靠，不会平白给模型加智力。超过某个推理难度，弱模型配什么工具都白搭。好在日常绝大多数任务到不了那个难度。所以那句话可能得改成：不是模型越强越好，而是合适的模型配上合适的工具，才是真好。反正对我这种干杂活的人来说，够用就行。
