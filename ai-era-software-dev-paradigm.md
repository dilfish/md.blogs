---
title: "AI时代的软件开发新范式"
date: 2026-08-08T21:30:00+08:00
draft: false
---

今年 8 月份，opencode 给了 deepseek v4 flash 一个大优惠，10 美元能买到 60 美元的额度，而且限时双倍，等于 10 美元换了 120 美元的额度。按 flash 模型的定价来算，这基本相当于给了几乎无穷无尽的额度，随便怎么烧都用不完。用起来的感觉，就是再也不用抠 token，再也不用算计一次对话能用多少字，想到什么就直接让它干。

这样的前提，带来了开发软件的新方式。以前看到好的项目、好的插件，最多是拿来用一用，看看源码，学学思路。现在额度管够，可以直接把整个项目丢给 AI，让它分析代码架构，然后完全按照自己喜欢的方式来重写。看不上它的目录结构，让它重排；不喜欢它的命名风格，让它重写；觉得它的架构不顺手，让它按你的习惯重构。反正成本几乎为零，想怎么改就怎么改，软件第一次变成了可以完全按个人口味定制的东西。

这个过程中也学会了当前流行的 coding agent 的工作方式，比如 plan + build 模式。先让 AI 出计划，看清楚它打算怎么干，然后让它动手实现。除了 planner 和 builder 这样的角色，还有 orchestrator 之类的角色，负责统筹调度各个子任务，把一个大需求拆分成小步骤，分配下去再汇总回来。这些角色分工，以前只能在公司的大工程团队里看到，现在一个人加一个 agent 就能复现整个流程。

市面上 zcode、pi.dev、command-code 这些 coding agent 我也研究过，最值得琢磨的不是它们怎么用，而是它们对 plan、build、verifier、orchestrator 这套角色体系在实现上怎么取舍。拿 plan + build 来说，最简单的实现是同一个模型跑两遍：先让它输出一份计划文本，人确认了，再让它写代码，计划只是提示词里的一个环节。稍微讲究一点的做法，是把 plan 和 build 拆成两个独立的子代理，各有各的系统提示词、各有各的上下文窗口，plan 的输出作为 build 的输入。这就像公司里的架构师和开发，各司其职，互不污染对方的上下文。

verifier 这个角色的设计差别更大。有的工具让 build 自己顺便验证，写完代码自己跑测试、自己看结果，等于裁判和运动员是同一个人。有的工具专门起一个独立的 verifier 子代理，用不同的提示词、甚至不同的模型来挑毛病。后一种实现的好处很明显：写代码的模型和审查的模型思路不同，容易发现盲区，就像写论文的人和审论文的人不能是同一个。代价是同样的活要烧两遍 token，额度不宽裕的时候就不太舍得这么干。

orchestrator 是这几层里实现最复杂的。它的职责是把一个大任务拆成若干子任务，分给多个子代理并行执行，再把结果拼起来。实现上有两种典型路线：一种是共享一个大上下文，所有子代理都在里面干活，实现简单，但上下文越大越容易互相干扰；另一种是每个子代理只拿到自己需要的那部分上下文，用文件系统或者消息通道传递中间结果，各干各的，最后 orchestrator 负责合并。后者更符合真实的工程协作，但对任务拆分和结果整合的要求高得多。

还有一个实现细节很关键，就是状态管理。做得粗糙的 agent，任务跑完之前所有中间状态都放在内存里，一中断就前功尽弃。做得讲究的，会把每一步对话、每一个中间产物都落到磁盘上变成文件，中断了还能接着跑。这种持久化设计让长任务变得可恢复、可复盘，甚至可以多进程共享同一份任务历史。谁的状态管理做得扎实，谁就能支撑更长时间、更复杂的任务。

opencode 的架构则是另一套玩法，理解了它，就理解了什么叫"自己改自己"。它本身是一个命令行工具，但核心是那套 agent 框架：模型可以通过配置文件、插件、以及 agent 定义来定制，而这些配置本身就是代码，存在项目里。所以你完全可以写一个插件，给 opencode 增加一个新工具；也可以改配置文件，换模型、调参数、加权限规则；甚至能定义自己的 agent 角色，告诉它在特定场景下该用什么样的提示词和工具集。最妙的是，这些改动不需要重新编译、不需要重启服务，改完就能生效。

"自己改自己"的实现方式也很有意思。opencode 的 agent 本身就有读写项目文件的能力，所以理论上它完全可以打开自己的配置文件、修改自己的插件代码、然后继续运行。遇到一个问题，它可以先看看自己是怎么配置的，觉得不合理就自己改掉。这正是 openclaw 那套自我迭代的思路——模型借助工具修改自身的运行环境，而不是困在每次对话都要重新开始的循环里。但 opencode 和 openclaw 最大的区别，就是中间有一个 plan 的周期：它把"改配置"这件事也当成一个任务来对待，先给出计划，人类确认了它才动手。等于既保留了自主进化的能力，又留了一道人性的闸门，不会出现 AI 自己越改越偏，最后完全脱离控制的情况。

最后说结论。有了几乎无限的算力额度，有了 plan + build 这样成熟的工作流，有了能自我改进又不失控的 agent，软件开发这个行业正在迎来真正的范式转变：AI 将辅助有经验的人类写出任何软件。以前写一个软件，靠的是团队、资源和时间；以后，只要你有足够的经验，知道什么该做什么不该做，AI 会替你把剩下的一切都干完。门槛不在"能不能写出来"，而在"知不知道要写什么"。
